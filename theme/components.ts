const Text = {
  variants: {
    navlink: {
      color: 'white',
      fontFamily: 'Work Sans, sans-serif',
      marginRight: '2rem',
    },
  },
};

const COMPONENTS = {
  Text,
};

export default COMPONENTS;
